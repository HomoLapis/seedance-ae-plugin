# This project does NOT use .env files at runtime — API keys are stored in
# the panel's localStorage and entered via the Settings UI.
#
# This file exists only as a heads-up for forks that might want to bake
# defaults in for internal builds. Do NOT commit a real key.

# SEEDANCE_ARK_KEY=sk-replace-me
# SEEDANCE_ZAI_KEY=
# SEEDANCE_FAL_KEY=
# SEEDANCE_ALIBABA_KEY=
